# Copy this file to .env.local for local development.
# BACKEND_URL is only needed in production (Render sets it).
# Locally, next.config.js falls back to http://localhost:8000.

# BACKEND_URL=https://mfaq-backend.onrender.com
